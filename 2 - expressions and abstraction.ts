const my_name: string = "Hello, World!";

// Expressions can be:
// variable name
// string
// number
// boolean
// another expression + "." + identifier
// expression + expression
// expression - expression

// DRY - Don't Repeat Yourself

// operator precedence


console.log(("Conor" + my_name).length);
console.log("Conor" + my_name.length);
